# roster_apenv activate
-------------
1.pip install virtualenv
2.python -m venv env-krysta
3.env-rosterap\Scripts\activate

# install libs
pip freeze > requirements.txt 
pip install -r requirements.txt